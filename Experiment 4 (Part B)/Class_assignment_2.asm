;Find LCM
CODE SEGMENT
ASSUME CS:CODE, DS:CODE
ORG 100H
MOV AX, 35
MOV BX, 7
PUSH AX
PUSH BX
MOV CX, 0FFFFH
LEV: XOR DX,DX
	DIV BX
	CMP DX, 0H
	JE END
	MOV AX, BX
	MOV BX, DX
	LOOP LEV
END:
MOV CX, BX
POP BX
POP AX
MUL BX
MOV BX, CX
DIV BX
; The result is in BX.